version: "3.9"
services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - NO_PROXY=genai-api.visa.com,localhost,127.0.0.1
      - no_proxy=genai-api.visa.com,localhost,127.0.0.1
    restart: unless-stopped
